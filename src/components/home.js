import React from 'react';
import FilteredBooks from './filteredBooks';

const Home = (props) => {
  return (
    <div className="container">
      <FilteredBooks />
    </div>
  );
};

export default Home;
